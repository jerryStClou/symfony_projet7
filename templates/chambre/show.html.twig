{% extends 'base.html.twig' %}

{% block title %}Chambre
{% endblock %}

{% block body %}

	<br><br>
	<div class="showChambre">
		<h1>{{ chambre.titre }}</h1>
		<br><br>
		{% for photo in chambre.photos %}
			<img src="{{asset('images/Chambres/'~ photo.reference)}}" alt="" style='width:60%'>
		{% endfor %}
		<br><br>
		<h2>Description courte :
			{{ chambre.descriptionCourte }}</h2>

		<br>


		<h2>Description long:
			{{ chambre.descriptionLongue}}</h2>
		<br>
		<h2>Prix journalier
			{{ chambre.prixJournalier}}€/nuit</h2>
		<br>
		<a href="{{ path('app_dateChoice', {'id': chambre.id}) }}" class="btn btn-dark">Reserver</a>
	</div>
	{# 
																																					    <table class="table">
																																					        <tbody>
																																					            <tr>
																																					                <th>Id</th>
																																					                <td>{{ chambre.id }}</td>
																																					            </tr>
																																					            <tr>
																																					                <th>Titre</th>
																																					                <td>{{ chambre.titre }}</td>
																																					            </tr>
																																					            <tr>
																																					                <th>Description_courte</th>
																																					                <td>{{ chambre.descriptionCourte }}</td>
																																					            </tr>
																																					            <tr>
																																					                <th>Description_longue</th>
																																					                <td>{{ chambre.descriptionLongue }}</td>
																																					            </tr>
																																					            <tr>
																																					                <th>Create_at</th>
																																					                <td>{{ chambre.createAt ? chambre.createAt|date('Y-m-d H:i:s') : '' }}</td>
																																					            </tr>
																																					        </tbody>
																																					    </table>
																																					
																																					    <a href="{{ path('app_chambre_index') }}">back to list</a>
																																					
																																					    <a href="{{ path('app_chambre_edit', {'id': chambre.id}) }}">edit</a> #}

	{# {{ include('chambre/_delete_form.html.twig') }} #}
{% endblock %}
